import { FunctionHttpError,functionErrorResponse,requestCorrelationId,requireAsteraActor,type AsteraFunctionEnv } from '../../../../_account-projection';import{undoDelete,StorageStoreError}from'../../../../_storage-store';type C={request:Request;env:AsteraFunctionEnv;params:{object?:string}};export async function onRequestPost(c:C){const cid=requestCorrelationId(c.request);try{const a=await requireAsteraActor(c.request,c.env),id=c.params.object?.trim();if(!id)throw new FunctionHttpError(400,'STORAGE_OBJECT_ID_REQUIRED','Storage Object IDが必要です。');return Response.json(await undoDelete(c.env.ASTERA_DB,{userId:a.user.id,tenantId:a.profile.tenant_id},id),{headers:{'Cache-Control':'no-store','X-Correlation-ID':cid}});}catch(e){const x=e instanceof StorageStoreError?new FunctionHttpError(e.status,e.code,e.message,e.details):e;return functionErrorResponse(x,cid);}}export function onRequest(c:C){return c.request.method==='POST'?onRequestPost(c):Promise.resolve(Response.json({error:{code:'METHOD_NOT_ALLOWED',message:'POSTのみ対応しています。'}},{status:405}));}
